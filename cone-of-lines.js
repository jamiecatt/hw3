// Lines: Alternative Code (Counter)

var counter = 10;
while (counter <= width) { 
  line (counter, 10, 10, counter);
  counter = counter + 5;
}

// Paremeters: (x, y, x, y)
// Increments: 5; x1 = y2; y1 = x2
